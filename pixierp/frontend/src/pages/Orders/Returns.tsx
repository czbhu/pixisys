import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const Returns = () => {
    return (
        <Card>
            <Title level={3}>Visszaküldések kezelése</Title>
            <p>Itt lesznek a visszaküldések kezelésére szolgáló funkciók.</p>
        </Card>
    );
};

export default Returns;
